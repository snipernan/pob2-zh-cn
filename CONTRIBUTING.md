# 参与贡献

请先阅读 README 和第三方来源说明。代码贡献按根目录 MIT 许可提交；游戏文本遵循原来源许可，不能将第三方文本声明为自己的原创代码。

## 文字修订

1. 界面用语修改 `translations.tsv`，一行一组英文和简体中文，中间一个制表符。
2. 游戏文本修改 `data/` 中对应映射，保留 ID、完整英文、来源链接、适用版本和 `verification`。
3. 已核验国服文本优先；编年史仅补缺。未经核验的新词保留英文，不批量模糊匹配。
4. 数值槽位、换行、正负号和条件须保持含义一致；人工校对注明原因。
5. 运行 `python3 build_assets.py`。如果增加新汉字，按开发指南重建 Noto 图集。

## 代码修订

运行 README 中的检查。涉及真实控件、计算输出或搜索时运行 Mac 核心集成测试；涉及原生输入需要真实窗口验证。不要将账户、配装、录屏、安装备份或本机路径提交到仓库。

PR 描述说明触发条件、前后行为、数据来源及实际运行的测试。不要只写“已测试”；注明未覆盖的系统／核心版本。
